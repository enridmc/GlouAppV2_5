// Placeholder for the future go_router setup.
//
// The current demo keeps navigation in MainShell because authentication and
// role permissions are still hardcoded. When backend auth arrives, routes such
// as /login, /dashboard, /tables, /qr/:token and /kitchen should move here.
